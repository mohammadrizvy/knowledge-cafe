import React from 'react';

const SpentTime = ({time}) => {
    
    console.log(time)
    return (
      <div>
        <h2>Hello form the other side {time}</h2>
      </div>
    );
};

export default SpentTime;